import type { Viewport } from "next";

export const appViewport: Viewport = {
    colorScheme: "dark",
    themeColor: "#07101b",
};

export const documentRootClassName = "dark";

export const documentBodyClassName =
    "min-h-screen bg-[var(--color-surface)] text-[var(--color-ink)] antialiased";
